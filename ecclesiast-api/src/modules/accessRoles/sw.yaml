components:
  schemas:
    AccessRole:
      type: object
      properties:
        name:
          type: string
      example:
        name: admin
  requestBodies:
    AccessRoleCreate:
      description: Create new access role body
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/AccessRole'